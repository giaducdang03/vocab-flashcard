# Đa ngôn ngữ (en/vi) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Người dùng đổi được giao diện web giữa tiếng Anh và tiếng Việt bằng một nút trên header, và thông báo lỗi từ máy chủ cũng đổi theo.

**Architecture:** `react-i18next` với các file JSON tách theo namespace, khởi tạo một lần trong `main.tsx`. Lựa chọn ngôn ngữ lưu ở `localStorage`, không đụng database. Backend đổi từ trả câu tiếng Anh sang trả mã lỗi có cấu trúc; frontend dịch mã đó tại một hàm duy nhất đã có sẵn.

**Tech Stack:** React 18, TypeScript, Vite, Tailwind, `i18next` + `react-i18next`; backend FastAPI + pytest.

**Spec:** `docs/superpowers/specs/2026-09-20-i18n-design.md`

## Global Constraints

- Ngôn ngữ mặc định là `en`. `fallbackLng: 'en'`.
- Khoá localStorage: `vocabflash.lang`. Giá trị hợp lệ: `'en'` | `'vi'`.
- Quy ước key: `<vùng>.<khối>.<ý>`, mỗi đoạn viết kiểu camelCase.
  Ví dụ `landing.hero.title`. **Không bao giờ** dùng câu tiếng Anh làm key.
- **Không** thêm test runner cho frontend. Không cài `vitest`, `jest`,
  `@testing-library/*`.
- **Không đụng** vào khu admin: `src/pages/AdminUsersPage.tsx`,
  `src/pages/AdminUserDetailPage.tsx`, `src/components/admin/**`,
  `src/lib/adminFormat.ts`. Chúng giữ nguyên tiếng Anh.
- **Không đụng** vào `src/content/changelog/*.md` (nội dung bài viết),
  prompt AI ở `backend/app/services/ai/quiz_prompt.py`, và email hệ thống.
- Không dịch tên thương hiệu `VocabFlash`.
- Không lazy-load namespace. Bundle thẳng cả hai ngôn ngữ.
- Câu có markup ở giữa (link, chữ đậm) phải dùng `<Trans>`. **Không** cắt câu
  thành nhiều mảnh rồi ghép chuỗi.
- Mọi task đều phải chạy `npm run build` thành công trước khi commit.

---

## File Structure

**Tạo mới (frontend):**

| File | Trách nhiệm |
|---|---|
| `src/i18n/index.ts` | Cấu hình i18next, đọc/ghi localStorage, đổi ngôn ngữ |
| `src/i18n/locales/en/*.json` | Bản tiếng Anh, một file mỗi namespace |
| `src/i18n/locales/vi/*.json` | Bản tiếng Việt, một file mỗi namespace |
| `src/lib/format.ts` | Định dạng ngày/số theo locale đang bật (ngoài admin) |
| `src/components/flags/FlagEn.tsx` | SVG Union Jack |
| `src/components/flags/FlagVi.tsx` | SVG cờ Việt Nam |
| `src/components/LanguageSwitcher.tsx` | Nút đổi ngôn ngữ độc lập |
| `scripts/check-i18n.mjs` | Đối chiếu key `en` ↔ `vi`, chạy trong `npm run build` |

**Tạo mới (backend):**

| File | Trách nhiệm |
|---|---|
| `backend/app/errors.py` | Danh mục mã lỗi + hàm `api_error()` |
| `backend/tests/test_errors.py` | Kiểm tra danh mục mã lỗi và hình dạng `detail` |

**Sửa:** `src/main.tsx`, `package.json`, và các file UI theo từng task.

---

## Task 1: Hạ tầng i18n, định dạng, và script đối chiếu key

**Files:**
- Modify: `frontend/package.json`
- Create: `frontend/src/i18n/index.ts`
- Create: `frontend/src/i18n/locales/en/common.json`
- Create: `frontend/src/i18n/locales/vi/common.json`
- Create: `frontend/src/lib/format.ts`
- Create: `frontend/scripts/check-i18n.mjs`
- Modify: `frontend/src/main.tsx`

**Interfaces:**
- Consumes: không có.
- Produces:
  - `src/i18n/index.ts` default export: instance `i18n` của i18next.
  - `export type AppLanguage = 'en' | 'vi'`
  - `export const LANGUAGES: readonly AppLanguage[]`
  - `export const LANG_STORAGE_KEY = 'vocabflash.lang'`
  - `export const DEFAULT_LANGUAGE: AppLanguage`
  - `export const INTL_LOCALE: Record<AppLanguage, string>`
  - `export function getStoredLanguage(): AppLanguage`
  - `export function setAppLanguage(lang: AppLanguage): void`
  - `src/lib/format.ts`:
    `export function useFormatters(): { date(iso: string): string; dateTime(iso: string): string; number(n: number): string }`

- [ ] **Step 1: Cài thư viện**

```bash
cd frontend
npm install i18next@^23.16.0 react-i18next@^15.1.0
```

- [ ] **Step 2: Tạo `src/i18n/index.ts`**

```ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';

import enCommon from './locales/en/common.json';
import viCommon from './locales/vi/common.json';

export type AppLanguage = 'en' | 'vi';

export const LANGUAGES: readonly AppLanguage[] = ['en', 'vi'] as const;
export const LANG_STORAGE_KEY = 'vocabflash.lang';
export const DEFAULT_LANGUAGE: AppLanguage = 'en';

/** Locale dùng cho Intl, suy ra từ ngôn ngữ app. */
export const INTL_LOCALE: Record<AppLanguage, string> = {
  en: 'en-US',
  vi: 'vi-VN',
};

function isAppLanguage(value: unknown): value is AppLanguage {
  return value === 'en' || value === 'vi';
}

/** Đọc ngôn ngữ đã lưu. localStorage có thể ném lỗi ở chế độ ẩn danh. */
export function getStoredLanguage(): AppLanguage {
  try {
    const stored = window.localStorage.getItem(LANG_STORAGE_KEY);
    if (isAppLanguage(stored)) {
      return stored;
    }
  } catch {
    // Bỏ qua: không đọc được thì dùng mặc định.
  }
  return DEFAULT_LANGUAGE;
}

export const resources = {
  en: { common: enCommon },
  vi: { common: viCommon },
} as const;

void i18n.use(initReactI18next).init({
  resources,
  lng: getStoredLanguage(),
  fallbackLng: DEFAULT_LANGUAGE,
  defaultNS: 'common',
  interpolation: { escapeValue: false },
});

/** Đổi ngôn ngữ, ghi nhớ lựa chọn, và cập nhật thuộc tính lang của trang. */
export function setAppLanguage(lang: AppLanguage): void {
  void i18n.changeLanguage(lang);
  document.documentElement.lang = lang;
  try {
    window.localStorage.setItem(LANG_STORAGE_KEY, lang);
  } catch {
    // Bỏ qua: không ghi được thì lựa chọn chỉ sống trong phiên này.
  }
}

document.documentElement.lang = i18n.language;

export default i18n;
```

- [ ] **Step 3: Tạo hai file `common.json`**

`src/i18n/locales/en/common.json`:

```json
{
  "loading": "Loading…",
  "save": "Save",
  "cancel": "Cancel",
  "delete": "Delete",
  "close": "Close",
  "back": "Back",
  "retry": "Try again",
  "language": "Language"
}
```

`src/i18n/locales/vi/common.json`:

```json
{
  "loading": "Đang tải…",
  "save": "Lưu",
  "cancel": "Huỷ",
  "delete": "Xoá",
  "close": "Đóng",
  "back": "Quay lại",
  "retry": "Thử lại",
  "language": "Ngôn ngữ"
}
```

- [ ] **Step 4: Tạo `src/lib/format.ts`**

Chỉ dùng cho phần **ngoài** admin. `lib/adminFormat.ts` giữ nguyên, không đụng.

```ts
import { useTranslation } from 'react-i18next';
import { INTL_LOCALE, type AppLanguage } from '../i18n';

function localeOf(language: string): string {
  const base = language.split('-')[0];
  return INTL_LOCALE[base as AppLanguage] ?? INTL_LOCALE.en;
}

/** Định dạng ngày/số theo ngôn ngữ đang bật. Dùng trong component. */
export function useFormatters() {
  const { i18n } = useTranslation();
  const locale = localeOf(i18n.language);

  return {
    date: (iso: string) =>
      new Date(iso).toLocaleDateString(locale, {
        month: 'short',
        day: 'numeric',
        year: 'numeric',
      }),
    dateTime: (iso: string) =>
      new Date(iso).toLocaleString(locale, {
        month: 'short',
        day: 'numeric',
        year: 'numeric',
        hour: '2-digit',
        minute: '2-digit',
      }),
    number: (n: number) => n.toLocaleString(locale),
  };
}
```

- [ ] **Step 5: Tạo `scripts/check-i18n.mjs`**

```js
// Đối chiếu key giữa hai ngôn ngữ. Thoát khác 0 nếu lệch, để chặn build.
import { readdirSync, readFileSync, existsSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const root = join(dirname(fileURLToPath(import.meta.url)), '..', 'src', 'i18n', 'locales');
const LANGS = ['en', 'vi'];

/** Trải JSON lồng nhau thành danh sách key phẳng: "a.b.c". */
function flatten(obj, prefix = '') {
  return Object.entries(obj).flatMap(([k, v]) => {
    const key = prefix ? `${prefix}.${k}` : k;
    return v && typeof v === 'object' && !Array.isArray(v) ? flatten(v, key) : [key];
  });
}

function keysOf(lang, file) {
  const path = join(root, lang, file);
  if (!existsSync(path)) return null;
  return new Set(flatten(JSON.parse(readFileSync(path, 'utf8'))));
}

const files = new Set(LANGS.flatMap((l) => readdirSync(join(root, l))));
const problems = [];

for (const file of [...files].sort()) {
  const [en, vi] = LANGS.map((l) => keysOf(l, file));
  if (!en) problems.push(`${file}: thiếu hẳn file bản en`);
  if (!vi) problems.push(`${file}: thiếu hẳn file bản vi`);
  if (!en || !vi) continue;

  for (const k of en) if (!vi.has(k)) problems.push(`${file}: vi thiếu key "${k}"`);
  for (const k of vi) if (!en.has(k)) problems.push(`${file}: en thiếu key "${k}"`);
}

if (problems.length) {
  console.error(
    `check-i18n: ${problems.length} vấn đề\n` + problems.map((p) => `  - ${p}`).join('\n'),
  );
  process.exit(1);
}
console.log(`check-i18n: OK, ${files.size} namespace khớp key giữa ${LANGS.join(' và ')}.`);
```

- [ ] **Step 6: Nối script vào `package.json`**

Trong `"scripts"`, thêm `check:i18n` và đổi `build`:

```json
"check:i18n": "node scripts/check-i18n.mjs",
"build": "node scripts/check-i18n.mjs && tsc -b && vite build"
```

- [ ] **Step 7: Chạy script để xác nhận nó báo OK**

Run: `cd frontend && npm run check:i18n`
Expected: `check-i18n: OK, 1 namespace khớp key giữa en và vi.`

- [ ] **Step 8: Chứng minh script bắt được lỗi thật**

Thêm tạm `"__probe": "x"` vào `src/i18n/locales/en/common.json`, chạy lại:

Run: `npm run check:i18n`
Expected: thoát mã 1, in `common.json: vi thiếu key "__probe"`

Xoá dòng `__probe` đi, chạy lại, phải OK. **Bước này bắt buộc** — một script kiểm tra chưa từng thất bại là một script chưa được kiểm chứng.

- [ ] **Step 9: Nạp i18n trong `src/main.tsx`**

Import phải nằm **trước** `import App`, vì i18n phải khởi tạo xong trước khi component đầu tiên render.

```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import './i18n';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>,
);
```

- [ ] **Step 10: Build**

Run: `cd frontend && npm run build`
Expected: script i18n in OK, `tsc -b` không lỗi, vite build thành công.

- [ ] **Step 11: Commit**

```bash
git add frontend/package.json frontend/package-lock.json frontend/scripts/check-i18n.mjs \
        frontend/src/i18n frontend/src/lib/format.ts frontend/src/main.tsx
git commit -m "feat(i18n): add i18next setup, locale formatters, and key parity check"
```

---

## Task 2: Cờ SVG và nút đổi ngôn ngữ

**Files:**
- Create: `frontend/src/components/flags/FlagEn.tsx`
- Create: `frontend/src/components/flags/FlagVi.tsx`
- Create: `frontend/src/components/LanguageSwitcher.tsx`

**Interfaces:**
- Consumes: `LANGUAGES`, `setAppLanguage`, `AppLanguage` từ `src/i18n` (Task 1).
- Produces:
  - `export default function LanguageSwitcher(): JSX.Element` — không nhận prop nào.
  - `export default function FlagEn({ className }: { className?: string })`
  - `export default function FlagVi({ className }: { className?: string })`

Không dùng emoji cờ. Windows không có glyph cờ quốc gia; Chrome và Edge trên Windows hiện `🇻🇳` thành hai chữ cái trong khung xám.

- [ ] **Step 1: Tạo `src/components/flags/FlagEn.tsx`**

```tsx
/** Cờ Anh (Union Jack). Kích thước do className quyết định. */
export default function FlagEn({ className = '' }: { className?: string }) {
  return (
    <svg viewBox="0 0 60 42" className={className} aria-hidden="true" focusable="false">
      <rect width="60" height="42" fill="#012169" />
      <path d="M0 0l60 42M60 0L0 42" stroke="#fff" strokeWidth="8" />
      <path d="M0 0l60 42M60 0L0 42" stroke="#C8102E" strokeWidth="4" />
      <path d="M30 0v42M0 21h60" stroke="#fff" strokeWidth="14" />
      <path d="M30 0v42M0 21h60" stroke="#C8102E" strokeWidth="8" />
    </svg>
  );
}
```

- [ ] **Step 2: Tạo `src/components/flags/FlagVi.tsx`**

```tsx
/** Cờ Việt Nam. Kích thước do className quyết định. */
export default function FlagVi({ className = '' }: { className?: string }) {
  return (
    <svg viewBox="0 0 30 20" className={className} aria-hidden="true" focusable="false">
      <rect width="30" height="20" fill="#DA251D" />
      <path
        fill="#FF0"
        d="M15 4.4l1.47 4.53h4.76l-3.85 2.8 1.47 4.53L15 13.46l-3.85 2.8 1.47-4.53-3.85-2.8h4.76z"
      />
    </svg>
  );
}
```

- [ ] **Step 3: Tạo `src/components/LanguageSwitcher.tsx`**

Dùng lại pattern click-outside đã có trong `src/components/UserMenu.tsx:16-26`.

```tsx
import { useEffect, useRef, useState } from 'react';
import { Check, ChevronDown } from 'lucide-react';
import { useTranslation } from 'react-i18next';
import { LANGUAGES, setAppLanguage, type AppLanguage } from '../i18n';
import FlagEn from './flags/FlagEn';
import FlagVi from './flags/FlagVi';

const LABEL: Record<AppLanguage, string> = { en: 'EN', vi: 'VI' };
const NAME: Record<AppLanguage, string> = { en: 'English', vi: 'Tiếng Việt' };

function Flag({ lang, className }: { lang: AppLanguage; className: string }) {
  return lang === 'en' ? <FlagEn className={className} /> : <FlagVi className={className} />;
}

export default function LanguageSwitcher() {
  const { t, i18n } = useTranslation();
  const [isOpen, setIsOpen] = useState(false);
  const menuRef = useRef<HTMLDivElement>(null);

  const current: AppLanguage = i18n.language.startsWith('vi') ? 'vi' : 'en';

  useEffect(() => {
    const handleClickOutside = (e: MouseEvent) => {
      if (menuRef.current && !menuRef.current.contains(e.target as Node)) {
        setIsOpen(false);
      }
    };

    if (isOpen) {
      document.addEventListener('mousedown', handleClickOutside);
      return () => document.removeEventListener('mousedown', handleClickOutside);
    }
  }, [isOpen]);

  const choose = (lang: AppLanguage) => {
    setAppLanguage(lang);
    setIsOpen(false);
  };

  return (
    <div className="relative" ref={menuRef}>
      <button
        type="button"
        onClick={() => setIsOpen(!isOpen)}
        aria-label={t('language')}
        aria-haspopup="listbox"
        aria-expanded={isOpen}
        className="inline-flex items-center gap-1.5 rounded-lg px-2 py-2 text-ink transition-colors hover:bg-canvas"
      >
        <Flag lang={current} className="h-3.5 w-5 rounded-[2px] ring-1 ring-hairline" />
        <span className="text-body-sm font-medium">{LABEL[current]}</span>
        <ChevronDown size={14} className="text-muted" />
      </button>

      {isOpen && (
        <div
          role="listbox"
          className="absolute right-0 z-50 mt-2 w-44 rounded-lg border border-hairline bg-white shadow-lg"
        >
          {LANGUAGES.map((lang) => (
            <button
              key={lang}
              type="button"
              role="option"
              aria-selected={lang === current}
              onClick={() => choose(lang)}
              className="flex w-full items-center gap-3 px-4 py-3 text-sm text-ink transition-colors first:rounded-t-lg last:rounded-b-lg hover:bg-canvas"
            >
              <Flag lang={lang} className="h-3.5 w-5 shrink-0 rounded-[2px] ring-1 ring-hairline" />
              <span className="flex-1 text-left">{NAME[lang]}</span>
              {lang === current && <Check size={16} className="text-primary" />}
            </button>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 4: Build**

Run: `cd frontend && npm run build`
Expected: thành công. Component chưa gắn vào đâu nên chưa nhìn thấy trên màn hình — Task 3 sẽ gắn.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/flags frontend/src/components/LanguageSwitcher.tsx
git commit -m "feat(i18n): add inline SVG flags and language switcher component"
```

---

## Task 3: Namespace `nav` và gắn nút đổi ngôn ngữ

Đây là task đầu tiên nhìn thấy được kết quả. Sau task này, đổi ngôn ngữ phải làm header, menu người dùng và footer đổi chữ ngay.

**Files:**
- Create: `frontend/src/i18n/locales/en/nav.json`
- Create: `frontend/src/i18n/locales/vi/nav.json`
- Modify: `frontend/src/i18n/index.ts` (đăng ký namespace `nav`)
- Modify: `frontend/src/components/PageHeader.tsx`
- Modify: `frontend/src/components/landing/LandingHeader.tsx`
- Modify: `frontend/src/components/UserMenu.tsx`
- Modify: `frontend/src/components/Footer.tsx`

**Interfaces:**
- Consumes: `LanguageSwitcher` (Task 2), i18n instance (Task 1).
- Produces: mẫu đăng ký namespace mà mọi task sau lặp lại y hệt.

- [ ] **Step 1: Tạo `src/i18n/locales/en/nav.json`**

```json
{
  "items": {
    "dashboard": "Dashboard",
    "sessions": "Sessions",
    "quizzes": "Quizzes"
  },
  "badgeNew": "New",
  "streak": "{{count}}-day streak",
  "landing": {
    "features": "Features",
    "method": "Method",
    "quizzes": "Quizzes",
    "pricing": "Pricing",
    "signIn": "Sign in",
    "getStarted": "Get started"
  },
  "user": {
    "menu": "User menu",
    "info": "User info",
    "userManagement": "User Management",
    "logout": "Logout"
  },
  "footer": {
    "tagline": "— Mindful vocabulary mastery.",
    "rights": "© 2026 ducdang. All rights reserved."
  }
}
```

- [ ] **Step 2: Tạo `src/i18n/locales/vi/nav.json`**

```json
{
  "items": {
    "dashboard": "Tổng quan",
    "sessions": "Bộ từ",
    "quizzes": "Đề kiểm tra"
  },
  "badgeNew": "Mới",
  "streak": "Chuỗi {{count}} ngày",
  "landing": {
    "features": "Tính năng",
    "method": "Phương pháp",
    "quizzes": "Đề kiểm tra",
    "pricing": "Bảng giá",
    "signIn": "Đăng nhập",
    "getStarted": "Bắt đầu"
  },
  "user": {
    "menu": "Menu người dùng",
    "info": "Thông tin tài khoản",
    "userManagement": "Quản lý người dùng",
    "logout": "Đăng xuất"
  },
  "footer": {
    "tagline": "— Học từ vựng có chủ đích.",
    "rights": "© 2026 ducdang. Bảo lưu mọi quyền."
  }
}
```

- [ ] **Step 3: Đăng ký namespace trong `src/i18n/index.ts`**

Thêm import và mở rộng `resources`:

```ts
import enNav from './locales/en/nav.json';
import viNav from './locales/vi/nav.json';

export const resources = {
  en: { common: enCommon, nav: enNav },
  vi: { common: viCommon, nav: viNav },
} as const;
```

- [ ] **Step 4: Sửa `src/components/PageHeader.tsx`**

`NAV_ITEMS` hiện nằm ở cấp module (dòng 12-16) nên chạy một lần lúc import — nó **không** thấy được ngôn ngữ đang bật. Đổi thành lưu key, dịch lúc render.

Thay khối `NAV_ITEMS`:

```tsx
const NAV_ITEMS = [
  { key: 'dashboard', path: '/' },
  { key: 'sessions', path: '/sessions' },
  { key: 'quizzes', path: '/quizzes', badge: true },
] as const;
```

Thêm hai import:

```tsx
import { useTranslation } from 'react-i18next';
import LanguageSwitcher from './LanguageSwitcher';
```

Thêm hook trong thân component, cạnh `useNavigate`:

```tsx
const { t } = useTranslation('nav');
```

Trong `<nav>`, thay `{item.label}` bằng:

```tsx
{t(`items.${item.key}`)}
```

và thay khối badge:

```tsx
{item.badge && (
  <span className="rounded-full bg-secondary-container px-1.5 py-0.5 text-caption-uppercase uppercase leading-none text-on-secondary-container">
    {t('badgeNew')}
  </span>
)}
```

Thay cả khối bên phải, chèn `<LanguageSwitcher />` trước `UserMenu` và dịch chuỗi streak:

```tsx
<div className="flex items-center gap-space-md">
  {typeof streakDays === 'number' && streakDays > 0 && (
    <span className="hidden items-center gap-1.5 rounded-full border border-hairline bg-surface-card px-2.5 py-1 font-mono text-code-sm text-ink sm:flex">
      <Flame size={14} className="text-primary" />
      {t('streak', { count: streakDays })}
    </span>
  )}
  <LanguageSwitcher />
  {user && <UserMenu user={user} onLogout={onLogout ?? (() => {})} />}
</div>
```

- [ ] **Step 5: Sửa `src/components/landing/LandingHeader.tsx`**

`NAV_ITEMS` ở dòng 5-10 cũng nằm ở cấp module — cùng cái bẫy. Đổi thành:

```tsx
const NAV_ITEMS = [
  { key: 'features', id: 'features' },
  { key: 'method', id: 'method' },
  { key: 'quizzes', id: 'quizzes' },
  { key: 'pricing', id: 'pricing' },
] as const;
```

Thêm import `useTranslation` từ `react-i18next` và `LanguageSwitcher` từ `'../LanguageSwitcher'`, thêm `const { t } = useTranslation('nav');`, đổi `{item.label}` thành ``{t(`landing.${item.key}`)}``.

Thay cả khối bên phải:

```tsx
<div className="flex items-center gap-space-md">
  <LanguageSwitcher />
  <Link
    to="/login"
    className="hidden px-space-xs py-space-sm text-button text-body transition-colors hover:text-ink sm:block"
  >
    {t('landing.signIn')}
  </Link>
  <Link
    to="/login"
    className="flex items-center justify-center rounded-lg bg-primary px-4 py-2.5 text-button text-on-primary transition-colors hover:bg-primary-active"
  >
    {t('landing.getStarted')}
  </Link>
  <Link
    to="/login"
    aria-label={t('landing.signIn')}
    className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-primary text-on-primary"
  >
    <User size={18} />
  </Link>
</div>
```

- [ ] **Step 6: Sửa `src/components/UserMenu.tsx`**

Thêm `import { useTranslation } from 'react-i18next';` và `const { t } = useTranslation('nav');`. Thay bốn chuỗi:

| Hiện tại | Thay bằng |
|---|---|
| `title="User menu"` | `title={t('user.menu')}` |
| `User info` | `{t('user.info')}` |
| `User Management` | `{t('user.userManagement')}` |
| `Logout` | `{t('user.logout')}` |

- [ ] **Step 7: Sửa `src/components/Footer.tsx`**

```tsx
import { useTranslation } from 'react-i18next';

export default function Footer() {
  const { t } = useTranslation('nav');

  return (
    <footer className="mt-auto w-full border-t border-hairline bg-canvas">
      <div className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-space-sm px-margin py-space-lg text-body-sm text-muted max-sm:px-space-md md:flex-row">
        <div className="flex items-center gap-space-sm">
          <span className="text-title-sm text-ink">VocabFlash</span>
          <span>{t('footer.tagline')}</span>
        </div>

        <span>{t('footer.rights')}</span>
      </div>
    </footer>
  );
}
```

- [ ] **Step 8: Đối chiếu key và build**

Run: `cd frontend && npm run build`
Expected: `check-i18n: OK, 2 namespace khớp key giữa en và vi.`, rồi build thành công.

- [ ] **Step 9: Kiểm tra bằng mắt trên app thật**

Run: `cd frontend && npm run dev`

Kiểm đủ năm điều:
1. Vào trang chủ khi chưa đăng nhập — nút `EN` có cờ hiện trên header.
2. Bấm nút, chọn `Tiếng Việt` — menu điều hướng, nút đăng nhập, footer đổi sang tiếng Việt **ngay**, không reload.
3. Tải lại trang (F5) — vẫn là tiếng Việt.
4. Mở DevTools, kiểm `document.documentElement.lang` bằng `vi`.
5. Đăng nhập, xem header trong app và menu người dùng cũng đã dịch.

Nếu cờ hiện ra thành ô vuông hay hai chữ cái thì SVG bị hỏng — sửa SVG, **không** được thay bằng emoji.

- [ ] **Step 10: Rà tràn chữ ở chế độ `vi`**

Chữ tiếng Việt dài hơn tiếng Anh 15-25%. Thu cửa sổ xuống 375px, kiểm header không vỡ, không tràn ngang. Sửa Tailwind class nếu cần.

- [ ] **Step 11: Commit**

```bash
git add frontend/src/i18n frontend/src/components/PageHeader.tsx \
        frontend/src/components/landing/LandingHeader.tsx \
        frontend/src/components/UserMenu.tsx frontend/src/components/Footer.tsx
git commit -m "feat(i18n): translate navigation chrome and mount language switcher"
```

---

## Quy trình chung cho Task 4 đến Task 9

Sáu task nội dung theo **đúng một quy trình giống hệt nhau**. Quy trình viết một lần ở đây; mỗi task bên dưới chỉ nêu danh sách file, tiền tố key, và những cái bẫy riêng của nó.

- [ ] **Bước A: Liệt kê chuỗi.** Đọc từng file trong danh sách của task, ghi ra mọi chuỗi người dùng nhìn thấy: chữ trong JSX, `placeholder`, `title`, `aria-label`, `alt` có nghĩa, nhãn nút, tiêu đề, thông báo khi rỗng, và chuỗi `fallback` truyền vào `apiErrorMessage`.
- [ ] **Bước B: Viết `locales/en/<ns>.json`** — chép nguyên văn chuỗi tiếng Anh đang có, đặt key theo tiền tố của task. Chuỗi có số đếm thì tách `_one` / `_other`.
- [ ] **Bước C: Viết `locales/vi/<ns>.json`** — cùng bộ key, dịch sang tiếng Việt. Tiếng Việt chỉ cần dạng `_other` cho key số nhiều; i18next tự xử lý phần còn lại.
- [ ] **Bước D: Đăng ký namespace** trong `src/i18n/index.ts`, y hệt Task 3 Step 3.
- [ ] **Bước E: Sửa từng file** — thêm `const { t } = useTranslation('<ns>');`, thay chuỗi bằng `t(...)`. Hằng số mảng ở cấp module phải đổi sang lưu key (mẫu ở Task 3 Step 4). Câu có link hay chữ đậm ở giữa dùng `<Trans>`. Ngày và số dùng `useFormatters()` từ `src/lib/format.ts`.
- [ ] **Bước F:** `npm run build` — phải in `check-i18n: OK` rồi build sạch.
- [ ] **Bước G:** `npm run dev`, đi hết các màn hình của namespace ở **cả hai** ngôn ngữ. Đổi qua lại EN/VI ngay trên màn hình đó, xác nhận chữ đổi tức thì và không còn chuỗi nào kẹt tiếng Anh.
- [ ] **Bước H:** Thu cửa sổ xuống 375px ở chế độ `vi`, rà tràn chữ và xuống dòng xấu.
- [ ] **Bước I: Commit** với thông điệp `feat(i18n): translate <ns> screens`.

---

### Task 4: Namespace `landing`

**Files:**
- Create: `frontend/src/i18n/locales/en/landing.json`, `frontend/src/i18n/locales/vi/landing.json`
- Modify: `frontend/src/i18n/index.ts`
- Modify: `frontend/src/pages/LandingPage.tsx`
- Modify trong `frontend/src/components/landing/`: `AnnouncementBar.tsx`, `HeroSection.tsx`, `HeroFlashcard.tsx`, `StatsStrip.tsx`, `PillarsSection.tsx`, `HowItWorks.tsx`, `PracticeArena.tsx`, `FinalCta.tsx`

`LandingHeader.tsx` **không** nằm ở đây — đã xong ở Task 3 dưới namespace `nav`.

**Interfaces:**
- Consumes: i18n instance (Task 1).
- Produces: namespace `landing` với các tiền tố `hero.*`, `announcement.*`, `stats.*`, `pillars.*`, `howItWorks.*`, `arena.*`, `finalCta.*`

**Bẫy riêng:**
- Đây là trang tiếp thị, chữ dài và giàu sắc thái nhất trong app. Dịch thoát ý, không dịch từng từ. `"Build vocabulary sessions that stick."` phải thành một câu tiếng Việt đọc tự nhiên, không phải bản dịch máy.
- `PillarsSection.tsx` và `HowItWorks.tsx` nhiều khả năng có mảng dữ liệu ở cấp module — đổi sang lưu key như Task 3 Step 4.
- `HeroSection.tsx:28` có chuỗi `AI-ASSISTED LEXICAL MASTERY` viết hoa toàn bộ. Bản `en` giữ nguyên dạng viết hoa; bản `vi` **không** viết hoa toàn bộ, vì tiếng Việt có dấu nên viết hoa hết rất khó đọc.

### Task 5: Namespace `auth`

**Files:**
- Create: `frontend/src/i18n/locales/en/auth.json`, `frontend/src/i18n/locales/vi/auth.json`
- Modify: `frontend/src/i18n/index.ts`, `frontend/src/pages/AuthPage.tsx`, `frontend/src/pages/AuthCallbackPage.tsx`

**Interfaces:**
- Consumes: i18n instance (Task 1), `LanguageSwitcher` (Task 2).
- Produces: namespace `auth` với tiền tố `login.*`, `register.*`, `verify.*`, `callback.*`, `field.*`, `validation.*`

**Bẫy riêng:**
- Trang này **không có header nào ở trên**, nên phải chèn `<LanguageSwitcher />` vào `AuthPage.tsx`, đặt góc trên bên phải. Bỏ sót bước này thì người vào thẳng `/login` không có cách nào đổi ngôn ngữ — và đó chính là màn hình đầu tiên của người dùng mới.
- Các chuỗi kiểm tra hợp lệ phía client (mật khẩu quá ngắn, email sai định dạng) cũng phải dịch, đừng chỉ dịch nhãn ô nhập.

### Task 6: Namespace `session`

**Files:**
- Create: `frontend/src/i18n/locales/en/session.json`, `frontend/src/i18n/locales/vi/session.json`
- Modify: `frontend/src/i18n/index.ts`
- Modify: `frontend/src/pages/SessionsPage.tsx`, `SessionDetailPage.tsx`, `StudyPage.tsx`, `PracticePage.tsx`
- Modify: `frontend/src/components/SessionCard.tsx`, `SessionCreateModal.tsx`, `ImportModal.tsx`, `QuestionTypeBadges.tsx`
- Modify trong `frontend/src/components/session/`: `AddCardModal.tsx`, `BulkActionBar.tsx`, `CardRow.tsx`, `CardsToolbar.tsx`, `PracticeSetupModal.tsx`, `PracticeSummary.tsx`

**Interfaces:**
- Consumes: i18n instance (Task 1), `useFormatters` từ `src/lib/format.ts` (Task 1).
- Produces: namespace `session` với tiền tố `list.*`, `detail.*`, `card.*`, `create.*`, `import.*`, `study.*`, `practice.*`, `toolbar.*`, `bulk.*`

**Bẫy riêng:**
- Task nặng nhất, khoảng 2.700 dòng. `StudyPage.tsx` (741 dòng) và `ImportModal.tsx` (558 dòng) chiếm quá nửa. Làm hai file này trước và commit riêng một lần, rồi mới làm phần còn lại — đừng để một commit ôm cả 14 file.
- `SessionCard.tsx:8` và `SessionDetailPage.tsx:19` gọi `toLocaleDateString(undefined, …)`, tức chạy theo locale trình duyệt chứ không theo ngôn ngữ app. Thay bằng `useFormatters().date(...)`.
- `BulkActionBar.tsx` có chuỗi đếm số thẻ đã chọn — dùng `_one` / `_other`.
- `StudyPage.tsx:68-69` lọc giọng đọc theo `v.lang.startsWith('en')`. Đó là **giọng đọc tiếng Anh của từ vựng**, hoàn toàn không liên quan đến ngôn ngữ giao diện. **Không được** đổi nó theo `i18n.language` — để nguyên.

### Task 7: Namespace `quiz`

**Files:**
- Create: `frontend/src/i18n/locales/en/quiz.json`, `frontend/src/i18n/locales/vi/quiz.json`
- Modify: `frontend/src/i18n/index.ts`
- Modify: `frontend/src/pages/QuizzesPage.tsx`, `QuizDetailPage.tsx`, `TakeQuizPage.tsx`, `AttemptReviewPage.tsx`
- Modify trong `frontend/src/components/quiz/`: `AttemptHistory.tsx`, `MasteryTrajectory.tsx`, `QuizCard.tsx`, `QuizCreateModal.tsx`, `QuizQuestionView.tsx`, `QuizStatCards.tsx`
- Modify: `frontend/src/utils/quizStats.ts`

**Interfaces:**
- Consumes: i18n instance (Task 1), `useFormatters` (Task 1).
- Produces: namespace `quiz` với tiền tố `list.*`, `detail.*`, `create.*`, `take.*`, `review.*`, `stats.*`, `history.*`

**Bẫy riêng:**
- `AttemptReviewPage.tsx:82` và `utils/quizStats.ts:105` hardcode `'en-US'`. Chuyển sang `useFormatters()`.
- `utils/quizStats.ts` **không phải component** nên không gọi hook được. Cho hàm đó nhận thêm tham số locale, hoặc chuyển chỗ định dạng lên component gọi nó. Đừng cố gọi hook trong file này — React sẽ ném lỗi lúc chạy.
- **Nội dung câu hỏi và giải thích do AI sinh ra không dịch.** Chúng là dữ liệu từ máy chủ và theo spec vẫn là tiếng Việt. Chỉ dịch phần khung: nhãn, nút, tiêu đề, trạng thái.

### Task 8: Namespace `dashboard`

**Files:**
- Create: `frontend/src/i18n/locales/en/dashboard.json`, `frontend/src/i18n/locales/vi/dashboard.json`
- Modify: `frontend/src/i18n/index.ts`, `frontend/src/pages/DashboardPage.tsx`
- Modify trong `frontend/src/components/dashboard/`: `DailyLearnedChart.tsx`, `KpiTile.tsx`, `SessionProgressChart.tsx`, `StatsSection.tsx`

`components/admin/ActivityBars.tsx` thuộc khu admin, **không** đụng vào.

**Interfaces:**
- Consumes: i18n instance (Task 1), `useFormatters` (Task 1).
- Produces: namespace `dashboard` với tiền tố `greeting.*`, `kpi.*`, `charts.*`, `stats.*`, `empty.*`

**Bẫy riêng:**
- Nhãn trục và chú giải của Chart.js nằm trong object cấu hình chứ không phải JSX — rất dễ bỏ sót vì grep JSX không thấy.
- Nhãn ngày trên trục hoành phải đi qua `useFormatters()`, nếu không biểu đồ sẽ hiện ngày tiếng Anh giữa giao diện tiếng Việt.
- Object cấu hình chart phải nằm **trong** thân component, hoặc bọc `useMemo` phụ thuộc `i18n.language`. Để ở cấp module là dính đúng cái bẫy `NAV_ITEMS` ở Task 3.

### Task 9: Namespace `changelog`

**Files:**
- Create: `frontend/src/i18n/locales/en/changelog.json`, `frontend/src/i18n/locales/vi/changelog.json`
- Modify: `frontend/src/i18n/index.ts`, `frontend/src/pages/ChangelogPage.tsx`
- Modify: `frontend/src/components/changelog/ChangelogSidebar.tsx`

**Interfaces:**
- Consumes: i18n instance (Task 1).
- Produces: namespace `changelog` với các key `title`, `sidebar.*`, `empty`, `roadmap.*`

**Bẫy riêng:**
- Chỉ dịch **khung trang**: tiêu đề, nhãn sidebar, nút. **Không đụng** vào `src/content/changelog/*.md` — nội dung bài viết giữ nguyên như đã viết.
- `components/changelog/Markdown.tsx` là bộ dựng hình, gần như chắc chắn không chứa chuỗi người dùng. Đọc kiểm lại rồi bỏ qua nếu đúng vậy.

---

## Task 10: Backend trả mã lỗi có cấu trúc

Task này TDD đầy đủ, vì backend đã có sẵn pytest.

**Files:**
- Create: `backend/app/errors.py`
- Create: `backend/tests/test_errors.py`
- Modify: `backend/app/deps.py`
- Modify: `backend/app/routers/admin.py`, `attempts.py`, `auth.py`, `cards.py`, `imports.py`, `quizzes.py`, `sessions.py`
- Modify: `backend/app/services/ai_policy.py`

**Interfaces:**
- Consumes: không có.
- Produces:
  - `class ErrorCode(str, Enum)` — danh mục mã lỗi.
  - `def api_error(status_code: int, code: ErrorCode, message: str, **params: Any) -> HTTPException`
  - Hình dạng `detail` mà Task 11 dựa vào: `{"code": str, "message": str, "params": dict}`

- [ ] **Step 1: Viết test thất bại trước**

Tạo `backend/tests/test_errors.py`:

```python
from fastapi import status

from app.errors import ErrorCode, api_error


def test_api_error_builds_structured_detail():
    exc = api_error(status.HTTP_404_NOT_FOUND, ErrorCode.QUIZ_NOT_FOUND, "Quiz not found")

    assert exc.status_code == status.HTTP_404_NOT_FOUND
    assert exc.detail == {
        "code": "quiz.not_found",
        "message": "Quiz not found",
        "params": {},
    }


def test_api_error_carries_params():
    exc = api_error(
        status.HTTP_400_BAD_REQUEST,
        ErrorCode.QUIZ_POOL_TOO_SMALL,
        "Need at least 8 cards to generate questions",
        min=8,
    )

    assert exc.detail["code"] == "quiz.pool_too_small"
    assert exc.detail["params"] == {"min": 8}


def test_error_codes_are_unique():
    values = [member.value for member in ErrorCode]
    assert len(values) == len(set(values)), "có mã lỗi bị trùng"


def test_error_codes_follow_domain_dot_reason():
    for member in ErrorCode:
        assert member.value.count(".") == 1, f"{member.name} phải có dạng <domain>.<reason>"
        assert member.value.islower(), f"{member.name} phải viết thường"
```

- [ ] **Step 2: Chạy test, xác nhận nó thất bại**

Run: `cd backend && python -m pytest tests/test_errors.py -v`
Expected: FAIL với `ModuleNotFoundError: No module named 'app.errors'`

- [ ] **Step 3: Viết `backend/app/errors.py`**

```python
"""Mã lỗi có cấu trúc để client tự dịch sang ngôn ngữ người dùng."""

from enum import Enum
from typing import Any

from fastapi import HTTPException


class ErrorCode(str, Enum):
    """Danh mục mã lỗi. Mỗi mã có dạng <domain>.<reason>, viết thường.

    Client ánh xạ mã này sang câu dịch trong namespace `errors`.
    Thêm mã mới thì phải thêm bản dịch ở cả en lẫn vi, nếu không
    scripts/check-i18n.mjs sẽ chặn build.
    """

    # auth
    NOT_AUTHENTICATED = "auth.not_authenticated"
    INVALID_TOKEN = "auth.invalid_token"
    INVALID_CREDENTIALS = "auth.invalid_credentials"
    INVALID_OR_EXPIRED_CODE = "auth.invalid_or_expired_code"
    EMAIL_ALREADY_REGISTERED = "auth.email_already_registered"
    ADMIN_REQUIRED = "auth.admin_required"

    # user
    USER_NOT_FOUND = "user.not_found"

    # session
    SESSION_NOT_FOUND = "session.not_found"
    SESSION_POOL_TOO_SMALL = "session.pool_too_small"

    # card
    CARD_NOT_FOUND = "card.not_found"

    # quiz
    QUIZ_NOT_FOUND = "quiz.not_found"
    QUIZ_POOL_TOO_SMALL = "quiz.pool_too_small"
    QUIZ_NO_MATCHING_CARDS = "quiz.no_matching_cards"
    QUIZ_NOT_ENOUGH_FOR_TYPES = "quiz.not_enough_for_types"
    QUIZ_GENERATION_FAILED = "quiz.generation_failed"
    QUIZ_STILL_GENERATING = "quiz.still_generating"
    QUIZ_RETRY_LIMIT = "quiz.retry_limit"
    QUIZ_NOT_AI_GENERATED = "quiz.not_ai_generated"

    # attempt
    ATTEMPT_NOT_FOUND = "attempt.not_found"
    ATTEMPT_ALREADY_SUBMITTED = "attempt.already_submitted"
    ATTEMPT_NOT_SUBMITTED = "attempt.not_submitted"
    QUESTION_NOT_IN_QUIZ = "attempt.question_not_in_quiz"
    QUESTION_ALREADY_ANSWERED = "attempt.question_already_answered"

    # import
    IMPORT_UNSUPPORTED_TYPE = "import.unsupported_type"
    IMPORT_PARSE_FAILED = "import.parse_failed"

    # ai
    AI_DISABLED = "ai.disabled"
    AI_QUOTA_EXCEEDED = "ai.quota_exceeded"


def api_error(status_code: int, code: ErrorCode, message: str, **params: Any) -> HTTPException:
    """Dựng HTTPException có detail máy đọc được.

    `message` là câu tiếng Anh, giữ làm lưới an toàn cho client cũ và cho
    trường hợp frontend chưa có bản dịch của mã này.
    `params` là các giá trị chèn vào câu dịch, ví dụ min=8.
    """
    return HTTPException(
        status_code=status_code,
        detail={"code": code.value, "message": message, "params": params},
    )
```

- [ ] **Step 4: Chạy test, xác nhận đã qua**

Run: `cd backend && python -m pytest tests/test_errors.py -v`
Expected: PASS cả 4 test.

- [ ] **Step 5: Commit phần nền**

```bash
git add backend/app/errors.py backend/tests/test_errors.py
git commit -m "feat(api): add structured error codes"
```

- [ ] **Step 6: Chuyển 45 chỗ ném lỗi sang `api_error`**

Làm lần lượt từng file, theo thứ tự: `app/deps.py`, `auth.py`, `sessions.py`, `cards.py`, `quizzes.py`, `attempts.py`, `imports.py`, `services/ai_policy.py`, `admin.py`.

Mẫu chuyển đổi, chuỗi không tham số:

```python
# trước
raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Quiz not found")

# sau
raise api_error(status.HTTP_404_NOT_FOUND, ErrorCode.QUIZ_NOT_FOUND, "Quiz not found")
```

Mẫu chuyển đổi, chuỗi có tham số — con số phải ra khỏi câu và vào `params`, vì vị trí của nó trong câu tiếng Việt khác tiếng Anh:

```python
# trước
raise HTTPException(
    status_code=status.HTTP_400_BAD_REQUEST,
    detail=f"Need at least {MIN_POOL_SIZE} cards to generate questions",
)

# sau
raise api_error(
    status.HTTP_400_BAD_REQUEST,
    ErrorCode.QUIZ_POOL_TOO_SMALL,
    f"Need at least {MIN_POOL_SIZE} cards to generate questions",
    min=MIN_POOL_SIZE,
)
```

Bốn chỗ hiện đang là **tiếng Việt** phải đổi `message` sang tiếng Anh, vì `message` là lưới an toàn tiếng Anh. Câu tiếng Việt của chúng chuyển vào file dịch ở Task 11:

| Chuỗi tiếng Việt hiện có | Mã | `message` tiếng Anh |
|---|---|---|
| `"Đề đang được soạn"` | `QUIZ_STILL_GENERATING` | `"Quiz is still being generated"` |
| `"Đề này đã thử lại quá 3 lần"` | `QUIZ_RETRY_LIMIT` | `"Retry limit reached for this quiz"` |
| `"Chỉ đề có phần AI mới soạn lại được"` | `QUIZ_NOT_AI_GENERATED` | `"Only AI-generated quizzes can be regenerated"` |
| `"Tính năng soạn đề bằng AI chưa được bật trên máy chủ"` | `AI_DISABLED` | `"AI quiz generation is not enabled on this server"` |

- [ ] **Step 7: Xác nhận không còn chỗ nào ném chuỗi trần**

Run: `cd backend && grep -rn 'HTTPException(' --include=*.py app | grep -v 'app/errors.py'`
Expected: không ra dòng nào. Còn dòng nào là chỗ bị bỏ sót.

- [ ] **Step 8: Chạy toàn bộ test backend**

Run: `cd backend && python -m pytest -q`
Expected: toàn bộ PASS. Test nào đang khẳng định `detail` là chuỗi sẽ đỏ — sửa nó sang kiểm `detail["code"]`. Đó là thay đổi đúng theo thiết kế, không phải hồi quy.

- [ ] **Step 9: Thử tay một lỗi thật**

Chạy backend, gọi một endpoint với id không tồn tại:

Run: `curl -i http://localhost:8000/api/quizzes/does-not-exist`
Expected: HTTP 404, body `{"detail":{"code":"quiz.not_found","message":"Quiz not found","params":{}}}`

- [ ] **Step 10: Commit**

```bash
git add backend/app backend/tests
git commit -m "refactor(api): raise structured error codes from all endpoints"
```

---

## Task 11: Frontend dịch mã lỗi

**Files:**
- Create: `frontend/src/i18n/locales/en/errors.json`, `frontend/src/i18n/locales/vi/errors.json`
- Modify: `frontend/src/i18n/index.ts`
- Modify: `frontend/src/api/errors.ts`

**Interfaces:**
- Consumes: hình dạng `detail` `{code, message, params}` từ Task 10; i18n instance từ Task 1.
- Produces: `apiErrorMessage(err: unknown, fallback: string): string` — **giữ nguyên chữ ký cũ**, nên 16 nơi gọi không phải sửa dòng nào.

- [ ] **Step 1: Tạo `src/i18n/locales/en/errors.json`**

Key phải khớp **chính xác** giá trị `ErrorCode` ở Task 10.

```json
{
  "auth": {
    "not_authenticated": "You are not signed in.",
    "invalid_token": "Your session has expired. Please sign in again.",
    "invalid_credentials": "Incorrect email or password.",
    "invalid_or_expired_code": "That code is invalid or has expired.",
    "email_already_registered": "This email is already registered.",
    "admin_required": "You do not have permission to do that."
  },
  "user": {
    "not_found": "User not found."
  },
  "session": {
    "not_found": "Session not found.",
    "pool_too_small": "You need at least {{min}} cards to practise."
  },
  "card": {
    "not_found": "Card not found."
  },
  "quiz": {
    "not_found": "Quiz not found.",
    "pool_too_small": "You need at least {{min}} cards to generate questions.",
    "no_matching_cards": "No cards match the selected pool.",
    "not_enough_for_types": "Not enough cards for the selected question types.",
    "generation_failed": "Could not generate questions with those settings.",
    "still_generating": "This quiz is still being generated.",
    "retry_limit": "This quiz has already been retried 3 times.",
    "not_ai_generated": "Only AI-generated quizzes can be regenerated."
  },
  "attempt": {
    "not_found": "Attempt not found.",
    "already_submitted": "This attempt has already been submitted.",
    "not_submitted": "This attempt has not been submitted yet.",
    "question_not_in_quiz": "That question does not belong to this quiz.",
    "question_already_answered": "You have already answered this question."
  },
  "import": {
    "unsupported_type": "Only .csv and .xlsx files are allowed.",
    "parse_failed": "Could not read that file."
  },
  "ai": {
    "disabled": "AI quiz generation is not enabled on this server.",
    "quota_exceeded": "You have used all {{limit}} AI quiz generations in the last 24 hours."
  }
}
```

- [ ] **Step 2: Tạo `src/i18n/locales/vi/errors.json`**

```json
{
  "auth": {
    "not_authenticated": "Bạn chưa đăng nhập.",
    "invalid_token": "Phiên đăng nhập đã hết hạn. Vui lòng đăng nhập lại.",
    "invalid_credentials": "Email hoặc mật khẩu không đúng.",
    "invalid_or_expired_code": "Mã không đúng hoặc đã hết hạn.",
    "email_already_registered": "Email này đã được đăng ký.",
    "admin_required": "Bạn không có quyền thực hiện thao tác này."
  },
  "user": {
    "not_found": "Không tìm thấy người dùng."
  },
  "session": {
    "not_found": "Không tìm thấy bộ từ.",
    "pool_too_small": "Cần ít nhất {{min}} thẻ để luyện tập."
  },
  "card": {
    "not_found": "Không tìm thấy thẻ từ."
  },
  "quiz": {
    "not_found": "Không tìm thấy đề.",
    "pool_too_small": "Cần ít nhất {{min}} thẻ để soạn câu hỏi.",
    "no_matching_cards": "Không có thẻ nào khớp với nhóm đã chọn.",
    "not_enough_for_types": "Không đủ thẻ cho các dạng câu hỏi đã chọn.",
    "generation_failed": "Không soạn được câu hỏi với thiết lập này.",
    "still_generating": "Đề đang được soạn.",
    "retry_limit": "Đề này đã thử lại quá 3 lần.",
    "not_ai_generated": "Chỉ đề có phần AI mới soạn lại được."
  },
  "attempt": {
    "not_found": "Không tìm thấy lượt làm bài.",
    "already_submitted": "Lượt làm bài này đã nộp rồi.",
    "not_submitted": "Lượt làm bài này chưa nộp.",
    "question_not_in_quiz": "Câu hỏi này không thuộc đề đang làm.",
    "question_already_answered": "Bạn đã trả lời câu này rồi."
  },
  "import": {
    "unsupported_type": "Chỉ chấp nhận tệp .csv và .xlsx.",
    "parse_failed": "Không đọc được tệp này."
  },
  "ai": {
    "disabled": "Tính năng soạn đề bằng AI chưa được bật trên máy chủ.",
    "quota_exceeded": "Bạn đã dùng hết {{limit}} lượt soạn đề bằng AI trong 24 giờ qua."
  }
}
```

- [ ] **Step 3: Đăng ký namespace `errors`**

Trong `src/i18n/index.ts`, y hệt Task 3 Step 3.

- [ ] **Step 4: Mở rộng `src/api/errors.ts`**

```ts
import axios from 'axios';
import i18n from '../i18n';

type StructuredDetail = {
  code?: unknown;
  message?: unknown;
  params?: Record<string, unknown>;
};

function detailOf(err: unknown): unknown {
  if (!axios.isAxiosError(err)) return undefined;
  return (err.response?.data as { detail?: unknown } | undefined)?.detail;
}

/**
 * Lấy câu lỗi hiển thị được từ response lỗi của API.
 *
 * Thứ tự ưu tiên:
 *   1. Có `code` và có bản dịch  -> câu dịch theo ngôn ngữ đang bật.
 *   2. Có `code` nhưng chưa dịch -> `message` tiếng Anh từ máy chủ.
 *   3. `detail` là chuỗi          -> dùng nguyên chuỗi (endpoint chưa chuyển).
 *   4. Không có gì                -> `fallback` do nơi gọi truyền vào.
 */
export function apiErrorMessage(err: unknown, fallback: string): string {
  const detail = detailOf(err);

  if (detail && typeof detail === 'object' && !Array.isArray(detail)) {
    const { code, message, params } = detail as StructuredDetail;

    if (typeof code === 'string' && code) {
      const translated = i18n.t(`errors:${code}`, { ...params, defaultValue: '' });
      if (translated) return translated;
    }

    if (typeof message === 'string' && message.trim()) return message;
  }

  if (typeof detail === 'string' && detail.trim()) return detail;

  return fallback;
}
```

Lưu ý: dùng `i18n.t` trực tiếp chứ không dùng hook, vì đây là hàm thường chứ không phải component. Hệ quả: đổi ngôn ngữ xong thì chỉ những lỗi **xảy ra sau đó** mới đổi ngữ; lỗi đang hiện trên màn hình giữ nguyên câu cũ. Đó là hành vi chấp nhận được, không cần xử lý thêm.

- [ ] **Step 5: Build**

Run: `cd frontend && npm run build`
Expected: `check-i18n: OK` với đủ mọi namespace, rồi build sạch.

- [ ] **Step 6: Thử tay cả bốn nhánh fallback**

Chạy cả backend lẫn frontend. Kiểm đủ bốn tình huống — đây là điểm quan trọng nhất của task, vì ba nhánh sau chỉ lộ ra khi có sự cố thật:

1. **Có bản dịch:** đăng nhập sai mật khẩu ở chế độ `vi` → hiện `"Email hoặc mật khẩu không đúng."` Đổi sang `en`, thử lại → `"Incorrect email or password."`
2. **Có mã, chưa dịch:** tạm đổi một key trong cả hai file `errors.json` (ví dụ `card.not_found` thành `card.not_found_x`), gây lỗi đó → phải hiện `message` tiếng Anh từ máy chủ, **không** hiện chuỗi `errors:card.not_found` trần. Hoàn tác sau khi kiểm.
3. **Có tham số:** thử soạn đề khi bộ từ quá ít thẻ → câu tiếng Việt phải có đúng con số, không phải chữ `{{min}}`.
4. **Fallback cuối:** bật DevTools → Offline, bấm một nút gọi API → hiện chuỗi `fallback` mà nơi gọi truyền vào, không phải màn hình trắng.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/i18n frontend/src/api/errors.ts
git commit -m "feat(i18n): translate backend error codes on the client"
```

---

## Task 12: Rà soát lần cuối

**Files:** không tạo file mới; sửa chỗ nào còn sót, cộng hai file tài liệu.

**Interfaces:**
- Consumes: mọi namespace từ Task 3 đến Task 11.
- Produces: không có API mới.

- [ ] **Step 1: Truy chuỗi tiếng Anh còn sót ngoài admin**

Run:

```bash
cd frontend/src && grep -rn '>[A-Z][a-z].\{3,\}<' --include=*.tsx . \
  | grep -v '/admin/' | grep -v 'Admin' | grep -v 'i18n/'
```

Đọc từng dòng ra. Mỗi dòng hoặc là chuỗi bị bỏ sót (phải sửa), hoặc là dữ liệu từ máy chủ / tên riêng (bỏ qua). Ghi lại lý do ở những dòng cố ý giữ nguyên.

- [ ] **Step 2: Truy `placeholder`, `title`, `aria-label` còn chữ cứng**

Run:

```bash
cd frontend/src && grep -rn 'placeholder="\|title="\|aria-label="' --include=*.tsx . \
  | grep -v '/admin/' | grep -v 'i18n/'
```

Ba thuộc tính này là chỗ hay sót nhất, vì mắt không nhìn thấy chúng trên màn hình.

- [ ] **Step 3: Truy locale hardcode còn sót ngoài admin**

Run:

```bash
cd frontend/src && grep -rn "toLocale.*'en-US'\|toLocale.*undefined" --include=*.tsx --include=*.ts . \
  | grep -v 'adminFormat\|/admin/'
```

Expected: không ra dòng nào. `lib/adminFormat.ts` và `components/admin/` ra kết quả là **đúng** — chúng cố ý khoá `en-US`.

- [ ] **Step 4: Truy hằng số cấp module có chứa chữ**

Run:

```bash
cd frontend/src && grep -rn "^const [A-Z_]* = \[" --include=*.tsx . | grep -v '/admin/'
```

Với mỗi mảng tìm được, mở ra xem có chuỗi người dùng nhìn thấy không. Nếu có mà chưa chuyển sang key thì nó sẽ đứng yên một ngôn ngữ vĩnh viễn dù người dùng đã đổi — đây là lỗi im lặng, không có gì báo.

- [ ] **Step 5: Đi hết app ở chế độ `vi`**

Đăng nhập rồi đi qua: trang chủ → đăng nhập → tổng quan → danh sách bộ từ → chi tiết bộ từ → nhập tệp → học → luyện nhanh → danh sách đề → tạo đề → làm đề → xem lại → changelog. Ở mỗi màn hình, đổi sang `en` rồi về `vi`, xác nhận chữ đổi tức thì.

- [ ] **Step 6: Rà tràn chữ ở 375px**

Chạy lại đúng lượt đi ở Step 5 với cửa sổ rộng 375px, chế độ `vi`. Chữ tiếng Việt dài hơn 15-25% nên nút và thẻ có chiều rộng cố định là chỗ hỏng trước nhất.

- [ ] **Step 7: Kiểm ngôn ngữ mặc định trên máy sạch**

Mở cửa sổ ẩn danh, vào app. Phải ra **tiếng Anh**, vì mặc định là `en` và localStorage đang rỗng.

- [ ] **Step 8: Cập nhật tài liệu**

Thêm vào `README.md` một mục ngắn: app hỗ trợ en/vi; thêm ngôn ngữ mới thì tạo thư mục trong `src/i18n/locales/` và ghi vào `LANGUAGES`; `npm run check:i18n` sẽ bắt key thiếu. Ghi rõ rằng khu admin và nội dung AI **cố ý** không dịch, kèm lý do, để người sau không tưởng là bug.

Thêm một mục vào `RELEASE_NOTES.md` theo đúng định dạng các bản trước.

- [ ] **Step 9: Build lần cuối và commit**

```bash
cd frontend && npm run build
cd ../backend && python -m pytest -q
```

```bash
git add -A
git commit -m "docs: document bilingual support and finish i18n sweep"
```
